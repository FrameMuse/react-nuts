import React from 'react';
import { render } from '@testing-library/react';
import { BasicPopup } from './popup.composition';

it('should be true', () => {
  // const { getByText } = render(<BasicPopup />);
  // const rendered = getByText('hello from Popup');
  expect(true).toBeTruthy();
});
